////  Recordings////  Created by Matthew on 2012-11-04.//  Copyright (c) 2012 __MyCompanyName__. All rights reserved.////package app.model {	import com.mhl.*;	import app.model.Entity	import app.model.AppModel;	import com.mhl.libs.recentActivity;	import flash.display.MovieClip;	import flash.display.Sprite;	import flash.utils.Dictionary; 	import fl.data.DataProvider;	import flash.data.SQLResult;	import flash.data.SQLConnection;	import flash.filesystem.File;	import flash.data.SQLStatement;	import flash.data.SQLConnection;	import flash.events.*;	import flash.events.EventDispatcher;	import flash.events.Event	 public class FeaturedMovies extends EntityData{		public function FeaturedMovies(model:AppModel) {		 super(model);		init();		}		override public function init():void{			var Featured:String = "SELECT parentTitle, entityType, title, p, description FROM movies WHERE newforRelevance='true' AND releaseYear = '2012'";						getData(Featured, Entity, 12);																}				override public function createMap():void {			pageCount++			for(var movie:* in result.data) {			trace(result.data[movie].title);			}			if (!result.complete)		        {		            selectStmt.next(2);		        }		};	}};