package app {	import app.model.*;	import app.model.entitydata.*;		import app.view.*;	import app.control.*;	import flash.display.MovieClip;	import flash.display.StageScaleMode;	import flash.display.StageAlign;	public class App extends MovieClip{		private var app_model:AppModel;		private var app_controller:AppControl;		public function App() {			//app defaults			this.stage.scaleMode= StageScaleMode.NO_SCALE;			this.stage.align = StageAlign.TOP_LEFT;						// Data			app_model = new AppModel();						var _featuredMovies:EntityData = new FeaturedMovies(app_model);			var _newReleases:EntityData = new NewReleases(app_model);			var _premiumMovies:EntityData = new PremiumMovies(app_model);			var _freeMovies:EntityData = new FreeMovies(app_model);						var _featuredTv:EntityData = new FeaturedTv(app_model);			var _topWatched:EntityData = new TopWatched(app_model);			var _topDrama:EntityData = new TopDrama(app_model);			var _kidsTv:EntityData = new KidsTv(app_model);								// Controller			app_controller = new AppControl(app_model); 			addChild(app_controller);						// Views			var _list:AppView = new List(app_model, app_controller, this);			var _grid:AppView = new Grid(app_model, app_controller, this);			var _guide:AppView = new Guide(app_model, app_controller, this);			var _video:AppView = new Video(app_model, app_controller, this);									app_controller.addView(_list);			app_controller.addView(_grid);			app_controller.addView(_guide);			app_controller.addView(_video);					/*		app_view.add(new List(model)); */										// add app_view as a listener to the app_model	/*		app_model.addEventListener(AppModel.UPDATE, app_view.update);				app_model.addEventListener(AppModel.UPDATE, app_controller.update);*/		}	}}